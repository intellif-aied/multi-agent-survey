# 学习使命扩展为四框架生命周期比较

用户加入 houmao 与 omnigent。课程将四者放在同一条生命周期光谱上：AgentTeams 调度长期 Worker 资源，houmao 管理可恢复的 Agent session authority，omnigent 以惰性 Child Session 形成运行树，AgentSpace 从持久化任务队列驱动执行。比较继续使用调度对象、状态所有权、通信通道、执行隔离和失败恢复这些固定维度；使命已同步到 [[MISSION.md]]。
